fn make_counter() {
    let mut count = 0;

    fn next() {
        count = count + 1;
        count
    }

    next
}

fn get_counter() {
    let counter = make_counter();
    counter()
}

let counter = make_counter();
println!(counter());
println!(counter());
println!(counter());
