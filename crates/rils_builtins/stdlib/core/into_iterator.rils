/// Conversion into an iterator.
pub trait IntoIterator {
    /// The yielded item type.
    type Item;

    /// Consumes a value and creates an iterator.
    #[runtime(core::sequence::into_iter)]
    fn into_iter(self) -> _;
}
